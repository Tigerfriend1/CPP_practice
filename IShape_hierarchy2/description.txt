OCP는 수정에는 닫혀있고, 확장에는 열려있어야 한다는 객체지향프로그래밍의 설계 원칙 중 하나이다.

IShape는 추상적으로 구현된 인터페이스로, 하위클래스에서 구체화된다.
그렇기 때문에 새로운 도형이 추가된다고 하더라도 IShape인터페이스를 구현하는 하위클래스만 추가하면 될 뿐 다른 클래스에 수정을 요하지 않는다.
그러므로 IShape와 하위클래스는 OCP를 만족한다고 할 수 있다.